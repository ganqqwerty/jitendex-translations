name: Deploy public dictionary demos

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Assemble the public site
        run: |
          mkdir -p _site/comparison
          rsync -a site-home/ _site/
          rsync -a --exclude='.openai/' --exclude='dist/' terra-luna-site/ _site/comparison/
          find _site/comparison -type f -name '*.html' -exec sed -i 's|href="/|href="/jp-ru-kolobok-dictionary/comparison/|g' {} +
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
